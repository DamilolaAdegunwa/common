////var connection = new signalR.HubConnectionBuilder()
////    .withUrl('/Home/Index')
////    .build();

////connection.on('receiveMessage', addMessageToChat);

////connection.start()
////    .catch(error => {
////        debugger;
////        console.error(error);
////    });

////function sendMessageToHub(msg) {
////    debugger;
////    connection.invoke('SendMessage', msg);
////}